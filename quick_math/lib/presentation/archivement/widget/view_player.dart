import 'package:flutter/cupertino.dart';

class ViewPlayerWidget extends StatelessWidget {
  const ViewPlayerWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
